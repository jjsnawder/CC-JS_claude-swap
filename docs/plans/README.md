Plans for work spanning more than one session or touching more than a handful of
files. One file per plan: `<YYYY-MM-DD>-<slug>.md`, written BEFORE the code and
kept current as things change. These are committed on purpose - the point is that
they survive a context reset, and their git history shows how the plan moved.
